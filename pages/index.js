import Header from '../components/Header'

const Index = () => (
  <div>
    <Header />
    <div className="container margin-top-med">
      <p>home, home on the page...</p>
    </div>
  </div>
);

export default Index;
